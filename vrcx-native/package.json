{
  "name": "vrcx-native",
  "main": "index.js",
  "version": "0.0.0",
  "scripts": {
    "install": "prebuild-install || node-gyp rebuild"
  },
  "dependencies": {
    "bindings": "^1.5.0"
  },
  "devDependencies": {
    "node-addon-api": "*",
    "node-gyp": "^7.1.2",
    "prebuild-install": "^6.0.0"
  }
}
