{
  "name": "vrcx-native",
  "main": "index.js",
  "version": "0.0.0",
  "scripts": {
    "install": "prebuild-install || node-gyp rebuild"
  },
  "dependencies": {
    "bindings": "^1.5.0",
    "node-addon-api": "*",
    "prebuild-install": "^6.0.0"
  }
}
